service:
  type: LoadBalancer

ingress:
  enabled: true
  annotations: {
    cert-manager.io/cluster-issuer: letsencrypt-prod
  }
  hosts:
    - host: "whoami.fog.chalko.com"
      paths:
        - "/"
  tls:
    - secretName: whoami-fo-chalko-com-tls
      hosts:
        - "whoami.fog.chalko.com"
